import React from 'react';
import { ResourceEvent } from 'src/core/types';
export interface Props {
    resourceListRef: (ele: HTMLUListElement) => void;
    renderResource?: (resourceEvent: ResourceEvent) => React.ReactElement;
    onToggleExpand?: (slotId: string) => void;
    resourceEvents: ResourceEvent[];
    resourceTableWidth: number;
    config: any;
}
declare class ResourceList extends React.Component<Props> {
    shouldComponentUpdate(nextProps: Readonly<Props>, nextState: Readonly<Record<string, unknown>>): boolean;
    renderResourceEvent(resourceEvent: ResourceEvent): JSX.Element[] | undefined;
    render(): JSX.Element;
}
export default ResourceList;
