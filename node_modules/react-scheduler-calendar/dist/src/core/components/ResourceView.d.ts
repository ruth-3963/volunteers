import { Component } from 'react';
import { SchedulerData } from 'src/core';
interface ResourceViewProps {
    schedulerData: SchedulerData;
    contentScrollbarHeight: number;
    toggleExpandFunc?: (...args: any[]) => any;
}
declare class ResourceView extends Component<ResourceViewProps> {
    handleClick: (toggleExpandFunc: ((...args: any[]) => any) | undefined, schedulerData: SchedulerData, item: any) => () => void;
    render(): JSX.Element;
}
export default ResourceView;
