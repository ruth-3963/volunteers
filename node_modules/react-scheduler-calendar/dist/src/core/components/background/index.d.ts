import moment from 'moment';
import React, { Component } from 'react';
import { configType } from 'src/core/constants/config';
import { Header, ResourceEvent } from 'src/core/types';
interface Props {
    resourceEvents: ResourceEvent[];
    headers: Header[];
    config: configType;
    cellWidth: number;
    width: number;
    localeMoment: typeof moment;
}
declare class Background extends Component<Props> {
    shouldComponentUpdate(nextProps: Readonly<Props>, nextState: Readonly<Record<string, unknown>>): boolean;
    getCellStyle(groupOnly: boolean | undefined, config: any, cellWidth: number): React.CSSProperties;
    renderBackgroundRows(): JSX.Element[];
    render(): JSX.Element;
}
export default Background;
