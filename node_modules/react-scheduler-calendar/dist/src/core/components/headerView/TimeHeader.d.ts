import moment from 'moment';
import React from 'react';
import { Header } from 'src/core/types';
export interface Props {
    timeHeaderRef: (ele: HTMLDivElement) => void;
    headers: Header[];
    cellUnit: number;
    localeMoment: typeof moment;
    cellWidth: number;
    minuteSteps: number;
    schedulerContainerWidth: number;
    config: any;
    schedulerContentWidth: number;
}
declare class TimeHeader extends React.Component<Props> {
    shouldComponentUpdate(nextProps: Readonly<Props>, nextState: Readonly<Record<string, unknown>>): boolean;
    render(): JSX.Element;
}
export default TimeHeader;
