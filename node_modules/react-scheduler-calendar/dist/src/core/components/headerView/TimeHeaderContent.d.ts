import moment from 'moment';
import React from 'react';
import { configType } from 'src/core/constants/config';
import { Header } from 'src/core/types';
export interface Props {
    headers: Header[];
    headerHeight: number;
    cellWidth: number;
    minuteStepsInHour: number;
    cellUnit: number;
    config: configType;
    localeMoment: typeof moment;
    nonAgendaCellHeaderTemplateResolver?: (...args: any[]) => any;
}
export declare class HeaderView extends React.Component<Props> {
    shouldComponentUpdate(nextProps: Readonly<Props>, nextState: Readonly<Record<string, unknown>>): boolean;
    render(): JSX.Element;
}
export default HeaderView;
